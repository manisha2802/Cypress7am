///<reference types="cypress"/>

describe('', () => {
    it('', () => {
        cy.visit("https://kitchen.applitools.com/ingredients/drag-and-drop")
        cy.get('#reset-button').first().its('index')
        cy.reload()
        
    })
})